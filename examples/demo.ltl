// examples/demo.ltl — runnable demo for ltl_ini_parse

import std::io
use ltl_ini_parse as lib

fn main() {
    io::println("ltl_ini_parse demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}